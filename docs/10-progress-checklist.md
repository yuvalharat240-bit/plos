# plos — Progress Checklist

> Granular, living tracker. `CLAUDE.md` has the narrative status; this file
> has the checkboxes. Update it as work actually completes — don't batch
> updates until the end of a session, since that's exactly how track gets
> lost across a compaction or a long gap.

## Phase 0 — Architecture (done, reconciled 2026-09-21)

- [x] ADR (`docs/02`)
- [x] System architecture (`docs/03`)
- [x] Database schema (`docs/04`)
- [x] Agent architecture (`docs/05`)
- [x] Threat model (`docs/06`)
- [x] Privacy model (`docs/07`)
- [x] MVP definition (`docs/08`)
- [x] Reconciliation pass — 4 definite inconsistencies fixed, 2 real gaps left open and documented (infra break-glass, malicious-document handling), 1 cosmetic note left alone

## Design phase (done 2026-09-21)

- [x] Brand & foundations artboard (mark geometry, color, type)
- [x] Home + Ask plos artboards, Liquid Glass applied per real HIG rules
- [x] [Design artifact](https://claude.ai/artifact/ChhVF6Jd4wp5TV2At4b3Pv) published

## iOS app (screens built 2026-09-21, wider than the MVP doc's scope — see `CLAUDE.md`)

- [x] `PlosDesignSystem` Swift package — tokens, `PlusMark`, `TodayStateGrid`, `InsightCard`, `GlassAskBar`
- [x] Login / Signup screens
- [x] 5-tab main app: Today, Calendar, Workouts, History, Me (+Settings/Meds/ConnectedApps/PrivacyData)
- [x] Verified: compiles (21 files, 0 errors) and runs (`swift run PlosPreviewApp`, 5+ s, no crash)
- [x] **`apps/ios/plos.xcodeproj` built with real Xcode 27 (2026-09-21)** — `xcodebuild -scheme plos -destination 'generic/platform=iOS Simulator' build` → **BUILD SUCCEEDED**; `PlosPreviewApp` (macOS) also builds via real `xcodebuild`. One real bug found and fixed: the hand-authored `project.pbxproj`'s `XCLocalSwiftPackageReference` used `relativePath = ../PlosDesignSystem` (resolves to `apps/PlosDesignSystem`, which doesn't exist) instead of `relativePath = PlosDesignSystem` (the actual sibling location, `apps/ios/PlosDesignSystem`) — exactly the kind of thing this verification step existed to catch.
- [ ] **Launch verification still pending**: no iOS Simulator runtime was installed (only device type profiles) — a download is in progress as of this note. Compiling is confirmed; actually running the app in a simulator and screenshotting it is not yet.
- [ ] Sign in with Apple capability enabled in Xcode Signing & Capabilities (needs a real Apple Developer team). **Same blocker now confirmed to also cover HealthKit** (Milestone 3) — verified directly, not assumed: the entitlement doesn't survive into the actual signed binary under no-team automatic signing.

## Phase 1 — Backend implementation ([plan](09-phase1-implementation-plan.md))

### Milestone 0 — Xcode verification (DONE 2026-09-21)
- [x] Project opens and builds with real Xcode — see iOS app section above for the bug found/fixed
- [x] Launch in a real simulator — done during the pre-Milestone-8 audit pass: booted an iPhone 17 (iOS 27) simulator and launched the built app for the first time ever. Found and fixed a second, separate real bug in the process: `apps/ios/plos/Info.plist` was missing `CFBundleIdentifier`/`CFBundleExecutable`/`CFBundlePackageType`/`CFBundleVersion`/`CFBundleShortVersionString`/`CFBundleName` — the app built cleanly but could never have been installed anywhere until this was fixed (full detail in the pre-Milestone-8 audit-pass section below). LoginView renders correctly (screenshot taken). **Not done**: visually confirming the rest of the flow (Sign up → 5 tabs) — those screens sit behind Sign-in-with-Apple/passkey auth, which this environment can't complete (no real Apple Developer team, existing known blocker).

### Milestone 1 — Backend bootstrap (DONE 2026-09-21)
- [x] `apps/api` NestJS project scaffolded (`package.json`, `tsconfig.json`, `tsconfig.spec.json`, `nest-cli.json`, `src/main.ts`, `src/app.module.ts`) — verified: `nest build` succeeds, compiled `app.module.js` loads cleanly under plain `node`
- [x] `TenantDatabaseService` — the `SET LOCAL app.current_user_id` / `set_config` RLS mechanism (docs/04 §10.1), integration-tested for real (see below), not just compiled
- [x] Migrations written and applied (22 files, `apps/api/migrations/001`-`022` at the time) — faithful to docs/04's exact DDL, reordered for FK dependencies. **Renumbered in Milestone 2** (022_embeddings.js → 025, to make room for three new auth-related migrations ahead of it) — see Milestone 2's entry below; the file count and reasoning here are otherwise unchanged. Two real doc/implementation gaps found and resolved during writing, not silently:
  - §10.2 claims every domain extension table "also carries" `user_id`, but the actual DDL (incl. the one table given as "full DDL") has no such column — resolved with join-based RLS policies to each table's 1:1 parent instead of inventing an undocumented column (`020_rls_policies.js`'s header). Worth folding back into `docs/04`.
  - The doc never specifies `plos_app`/`plos_worker`'s table-level GRANTs beyond the `audit_log` example — added `021_role_grants.js` (broad-but-functional for Milestone 1, explicitly flagged for Milestone 7 to tighten).
- [x] `embedded-postgres`-based verification (`test/rls.e2e-spec.ts`, run via `npm run test:rls`) — no Docker/Postgres binary in this environment, so this is the real, no-Docker-needed path. Two real tooling bugs hit and fixed along the way: Jest's default `testMatch` doesn't match `*.e2e-spec.ts` (fixed with a dedicated `test/jest-e2e.json`, the standard NestJS convention); `embedded-postgres` is pure-ESM and ts-jest's CommonJS downlevel of `await import()` can't load it (fixed with a `Function`-constructed dynamic import, the standard workaround, plus `NODE_OPTIONS=--experimental-vm-modules` baked into the `test:rls` script via `cross-env`).
- [x] **Real cross-tenant RLS test passing — 6/6 tests, genuine verification against a real running Postgres**: user reads their own `events`/`event_fitness_session` rows; a different user is blocked from both (proving the direct-`user_id` policy AND the join-based policy for the extension-table gap above); a connection with `app.current_user_id` never set at all sees zero rows (fails safe); an `INSERT` lying about its `user_id` is rejected by `WITH CHECK`; `audit_log` stays append-only for `plos_app` even after the broad 021 grant. The pgvector-unavailable tolerance for migration 022 (embeddings) also fired exactly as designed and didn't block anything else.
- [~] `npm audit`: 7 high-severity advisories, all in dev/build tooling transitive deps (`@nestjs/cli`, `glob`, `picomatch`, `tmp` — build-time only) or in `multer` (bundled with `@nestjs/platform-express`, unused — no file-upload route exists yet). Not blocking Milestone 1 (no exploitable runtime path exists yet); re-check `multer` specifically before Milestone 6 wires up document uploads, and do a general `npm audit fix` pass in Milestone 7.

### Milestone 2 — Auth + journal/workout/med CRUD (backend DONE 2026-09-21; iOS wiring not started)
- [x] **Real gaps found while implementing, fixed, not worked around**:
  - `audit_log` had only a `FOR SELECT` RLS policy (018) — no policy at all covered `INSERT`, so every real write's audit-log call would have been silently denied by RLS the first time any endpoint actually tried it (Milestone 1's own audit_log test only ever inserted as the superuser, which bypasses RLS, so this never surfaced until now). Fixed: `022_audit_log_write_policy.js` adds the missing `WITH CHECK` INSERT policy.
  - No RLS-compatible way exists to look up a `sessions` row by refresh-token hash or a `webauthn_credentials` row by credential ID — both are exactly the "prove who you are with an opaque secret before you have a `user_id` to scope by" case, structurally incompatible with `runAsUser`. Fixed with two narrow `SECURITY DEFINER` lookup functions (`023_auth_lookup_functions.js`), each a single fixed parameterized query, `plos_app` granted `EXECUTE` only — flagged there for Milestone 8 since RDS's master user needs `BYPASSRLS` granted explicitly for these to keep working in production.
  - Those same two functions were initially declared `RETURNS sessions`/`RETURNS webauthn_credentials` (a bare composite type) instead of `RETURNS SETOF` — a real bug the e2e test caught: a SQL function with a non-`SETOF` return type that matches zero rows still returns exactly one all-`NULL` row, so a stale/rotated refresh token was silently treated as "found" with a `null` user id, crashing three calls deep with a Postgres UUID-syntax error instead of a clean 401. Fixed by switching both to `RETURNS SETOF`.
  - `docs/07-privacy-model.md`'s permission-scope catalog (seeded in `007`) had no `mental_health.write` scope despite journal entries needing one — added via `024_permission_scopes_mvp_additions.js` rather than reusing `mental_health.read_raw` (a read scope) or inventing an unlisted string.
- [x] **Real, documented interpretation of an underspecified area**: Phase 0 never says how a user's own `permission_grants` rows come to exist (Milestone 6 owns the consent/grant UI). Since Milestone 2 needs `ScopeGuard` to check something real, first-time Apple sign-in now self-grants every catalog scope except `professional.share` (risk_tier 3, needs an explicit sharing flow that doesn't exist yet) — `UserAccountService.grantMvpSelfScopes`. Revisit once Milestone 6 builds real consent UI; this is the load-bearing assumption if that milestone changes it.
- [x] **Sign in with Apple** (`src/auth/apple-identity.service.ts`) — real `jose`-based JWT verification (signature, issuer, audience, expiry) against an injectable JWKS source (`APPLE_JWKS_RESOLVER`), so the e2e test exercises the actual verification code path against a locally-generated RSA keypair instead of mocking verification away. `createRemoteJWKSet(https://appleid.apple.com/auth/keys)` is the real provider; cannot be exercised against genuine Apple-issued tokens yet since `APPLE_AUDIENCE` (the Apple Developer Service ID/bundle ID) isn't provisioned — same open item as the iOS "Known open items" below.
- [x] **Passkeys** (`src/auth/passkey.service.ts`, `@simplewebauthn/server`) — registration (authenticated, discoverable/resident-key credential) and login (unauthenticated, no username field) ceremonies, both fully verified end-to-end in the e2e suite using a real virtual FIDO2 authenticator (`test/support/virtual-authenticator.ts`: a genuine P-256 keypair producing spec-shaped CBOR attestation objects and ECDSA signatures via Node's own `crypto` + `cbor` — not a mocked ceremony). Includes signature-counter-non-advance rejection (clone detection) and single-use challenge enforcement.
- [x] **`AuthGuard` + `ScopeGuard` + `EntitlementGuard`** (docs/03 §2.3's exact pipeline) — `AuthGuard` verifies the access JWT AND does a live DB check that the backing `sessions` row isn't revoked (per the doc's explicit requirement, not just JWT expiry); `ScopeGuard` checks a route's `@RequiredScope()` against the caller's real `permission_grants` rows (proven by the e2e test: revoking one scope via direct DB update makes the corresponding endpoint start returning 403); `EntitlementGuard` is the documented pass-through (08 §9.2) — present, gates nothing (RevenueCat isn't integrated yet).
- [x] **Plain CRUD surface** — journal entries (`POST /v1/journal`, `mental_health.write`), workout logging (`POST /v1/workouts`, `fitness.write`, with nested exercise sets), medication dose logging (`POST /v1/medications`, `health.write`, `executed_by` hardcoded to `'user'`, `risk_tier=1`). Confirmed for real (not just asserted): zero agent-tool code exists anywhere in this repository as of this milestone, so 04 §14's "no Scoped Tool ever writes `action_medication_log`" cross-check is trivially true today — re-verify this once Milestone 5 adds the Scoped Tool registry, don't assume the comment in `medications.service.ts` is still accurate by then.
- [x] **Real HTTP-level verification, not just unit tests**: `test/auth-and-crud.e2e-spec.ts` boots the actual `AppModule` via `@nestjs/testing` + `supertest`, against the same real `embedded-postgres` approach as Milestone 1 — signup + idempotent re-signin, MVP auto-grant, 401 on missing/garbage bearer token, all three CRUD writes producing real RLS-scoped rows (verified via direct superuser query) plus real `audit_log` rows for each (the exact case the `audit_log` INSERT-policy gap above would have silently broken), scope revocation → 403, refresh-token rotation + old-token invalidation, logout → session revoked → subsequent request with the same still-unexpired JWT correctly rejected, full passkey register→login round trip, passkey replay rejection, onboarding profile/goal update, and onboarding input validation. **20/20 tests passing** (`npm run test:rls`, both suites combined). `nest build` still succeeds; `npm audit` unchanged (22 advisories, all pre-existing dev-tooling, none in the new runtime deps).
- [x] **iOS wiring — done 2026-09-21** (unblocked mid-milestone when real Xcode became available): `Networking/PlosAPI.swift` (URLSession client for every Milestone 2 endpoint), `Networking/KeychainStore.swift` (refresh-token persistence only — access tokens stay in memory), `Networking/PasskeyCeremony.swift` + `WebAuthnEncoding.swift` (real `ASAuthorizationController` bridging to async/await, byte-for-byte matching the backend's WebAuthn DTOs). `AppSession` rewritten with a real `AuthStage` state machine (`restoring`/`loggedOut`/`authenticating`/`signingUp`/`loggedIn`/`failed`), session restoration on launch, real Apple/passkey sign-in. `LoginView` extracts the real `ASAuthorizationAppleIDCredential` identity token (previously discarded); requests only the `.email` Apple scope, not `.fullName`, since nothing reads it — a direct application of CLAUDE.md's "no permission requested without a feature that needs it right now." `SignUpView` calls the new `PATCH /v1/users/me` (backend addition, see above) to set display name + create a primary goal, replacing the previous silent-discard. New `Journal/JournalView.swift` screen (free-text or structured mood/stress/energy check-in), reachable from a toolbar button on Home. `LogWorkoutSheet`/`LogDoseSheet` Save buttons now call the real endpoints instead of just `dismiss()`.
  - **Real gap fixed while doing this, found only because real Xcode now exists**: the hand-authored `plos.xcodeproj`'s local Swift package reference used `relativePath = ../PlosDesignSystem` — one directory too high (resolves to `apps/PlosDesignSystem`, which doesn't exist; the real location is `apps/ios/PlosDesignSystem`, a sibling of the `.xcodeproj`, not its parent's sibling). Fixed; this was exactly the class of bug Milestone 0 existed to catch.
  - **Verified for real**: both `xcodebuild -scheme plos -destination 'generic/platform=iOS Simulator' build` (the actual app target) and `swift build` (the package, all targets including `PlosPreviewApp`) succeed with zero errors after every change above. `swift run PlosPreviewApp` also still runs without crashing (session-restoration's "no stored token" path exercised for real, since `PlosPreviewApp` has no backend running).
  - **Not verified**: actually launching in an iOS Simulator and visually driving the flow — no simulator runtime was installed; a download was in progress as of this note (see Milestone 0). Passkey register/login cannot complete end-to-end regardless of simulator availability — needs an Associated Domains entitlement backed by a real hosted domain, which doesn't exist (documented in `PasskeyCeremony.swift`'s header, same blocker category as "no real Apple Developer team yet").
  - **Known UX gap, not silently smoothed over**: Workouts/Meds screens still *display* `MockData` lists — only the Save actions write real rows now. A saved workout/dose won't appear in that list until a real read/list endpoint exists (out of Milestone 2's scope, see the Context note above).
- [ ] Fold the `mental_health.write` scope addition and the extension-table RLS gap (Milestone 1) back into `docs/04`/`docs/07` themselves — still only documented in migration files, CLAUDE.md, and here.

**Client-platform scope changed 2026-09-21, mid-milestone (direct instruction)**: plos now targets PC/Linux/Windows (new: `apps/web`, not started), Android (new: `apps/android`, not started), and Apple (unchanged: `apps/ios`, extended with a planned Watch companion). See `docs/02-architecture-decision-record.md`'s D1 "Superseded in part" and D9 extension for the full reasoning. Nothing above needed to change for this — the backend was always a plain HTTP/JSON API. Sequencing for the two new clients hasn't been decided.

### Milestone 3 — HealthKit + Calendar sync (DONE 2026-09-21)
- [x] **Real gap found and fixed before any feature code**: two migrations needed adding — `025_productivity_write_scope.js` (the permission catalog had `productivity.read` but no write scope for the domain that calendar sync actually writes to; same category of gap as Milestone 2's `mental_health.write`), and `026_embeddings.js` renumbered (again) to stay last, since it's the one migration expected to fail without pgvector and the runner halts on first failure — the exact ordering bug from Milestones 1 and 2, caught before it recurred this time.
- [x] **Backend**: `src/sync/` — `POST /v1/sync/health` (`health.write`) and `POST /v1/sync/calendar` (`productivity.write`), both `AuthGuard`+`ScopeGuard`. `ImportService.insertSpineIfNew` makes every sync idempotent using `core_objects`'s existing `uq_core_objects_provider_source` unique index (`provider, source, source_id`) keyed on HealthKit's/EventKit's own stable object identifiers — re-syncing the same sample twice produces zero new rows, not duplicates or an error. `is_imported=true`/`provider='healthkit'`|`'apple_calendar'` set correctly on every synced row (docs/03 §2.1's stated provenance requirement). Calendar sync deliberately stores no event title — docs/08 §2.4 frames calendar as a density signal, not content; `events.title` stays `NULL` for every synced row, matching the client's own permission-usage string. Medication/journal/workout domains reuse the same tables Milestone 2 already built; sync just adds the `is_imported`-provenance write path alongside Milestone 2's `is_user_entered` one.
- [x] **Verified for real**: `test/sync.e2e-spec.ts` (real HTTP, real embedded Postgres, same harness as Milestones 1-2) — sleep/vital/workout samples land in correctly-provenanced rows; **re-syncing the exact same payload is proven idempotent** (`{inserted:0, skipped:3}` on the second call, row count unchanged); an invalid `vitalType` gets a clean 400, not a raw Postgres CHECK violation; `health.write` revocation → 403; calendar sync stores no title and is also idempotent. **26/26 tests passing** across all three e2e suites combined.
- [x] **Real, non-obvious tooling bug found and fixed while adding a third e2e spec file**: running all three suites together under `--runInBand` (single shared process) intermittently failed with "Test environment has been torn down," with one file's stack trace pointing at a *different* file's dynamically-constructed `import()` shim — a genuine Jest/V8 cross-file interaction from three files sharing byte-identical `new Function('specifier', 'return import(specifier)')` source text. Fixed by dropping `--runInBand` from `test:rls` — each spec file already uses its own port and fully independent `embedded-postgres` lifecycle, so running them as separate worker processes (Jest's default) gives real OS-level isolation instead. Documented in `rls.e2e-spec.ts`'s header for whoever adds a fourth spec file.
- [x] **`apps/api/scripts/dev-db.js` bug found and fixed**: the persistent dev database (added during the gap audit) called `pg.initialise()` unconditionally, which fails against an already-initialized data directory on every restart after the first. Fixed to only initialise on first run.
- [x] **iOS**: `DeviceSync/HealthKitService.swift` (real `HKHealthStore` queries for sleep/resting-HR/workouts, `#if os(iOS)`-gated with a macOS stub since `PlosDesignSystem` also builds for `PlosPreviewApp`), `DeviceSync/EventKitService.swift` (real `EKEventStore`, works on both platforms natively). `ConnectedAppsView` rewritten: both rows are real now, not hardcoded — tapping either requests the real system permission and syncs recent data through `PlosAPI`. Real HealthKit entitlement (`plos/plos.entitlements`) and both usage-description strings (`NSHealthShareUsageDescription`, `NSCalendarsFullAccessUsageDescription`) added and confirmed baked into the compiled Info.plist.
- [x] **Real, honest limitation documented, not glossed over**: `HKHealthStore.authorizationStatus` never reveals true grant/deny status for *read* types — Apple's own privacy design, not a bug here — so `ConnectedAppsView`'s Apple Health row can only mean "plos asked," never "plos was allowed," and says so in its own footer text. EventKit's status is exact and used as such.
- [x] **Real platform limitation found and confirmed, not assumed**: the HealthKit entitlement does not survive into the actual signed binary under this project's current no-team automatic signing — verified directly by inspecting the `.xcent` file `codesign` actually used (empty `<dict/>`, despite `plos.entitlements` correctly declaring the capability and a *different*, unused `-Simulated.xcent` intermediate showing it correctly). Tried and ruled out a targeted fix (forcing ad-hoc `CODE_SIGN_IDENTITY` for the simulator SDK specifically) before concluding this needs a real Apple Developer Team — the same blocker category as Sign in with Apple's, now confirmed to also cover HealthKit. **HealthKit authorization will likely fail at runtime until a real team is configured**; nothing about the Swift code itself is in question.
- [ ] **Not yet verified**: an actual on-device/simulator HealthKit or EventKit permission prompt and sync round-trip — blocked on the simulator runtime (Milestone 0) for EventKit, and additionally on a real Developer Team for HealthKit specifically, per the note above.
### Milestone 4 — Personal Baseline Engine / worker (DONE 2026-09-21)
- [x] **Real deployable design decision, made explicitly**: `docs/03` calls the worker a separate Fargate task, but there's no AWS deployment until Milestone 8 — built as a headless entry point sharing `apps/api`'s codebase/providers (`src/worker-main.ts`, `NestFactory.createApplicationContext`, no HTTP listener), matching the plan's own "a scheduled job, nightly is enough" framing. `npm run worker` (built) / `npm run worker:dev` (ts-node); a real scheduler triggers it repeatedly in production, not built here.
- [x] **`src/worker/`** — `BaselineService.computeForUser(userId)` is the only entry point, and opens exactly one fresh `runAsUser` transaction per call (`metric-definitions.ts` + `baseline.service.ts`). Four metrics, not three — "mood/stress" in the plan names one domain conversationally, not one column, and `baselines`' own `(user_id, domain, metric, window_days)` uniqueness needs them as two separate rows: `health/sleep_duration_min`, `fitness/training_load`, `mental_health/mood_score`, `mental_health/stress_score`. 14-day rolling window, minimum 3 samples to compute anything (fewer → the metric is skipped, no row written — a `baselines` row is a claim, not a stub).
- [x] **Real, documented interpretation of an underspecified area**: `docs/04`'s six-value classification enum was never given an algorithm. Built a genuine, if deliberately simple ("no scientific-computing stack" per ADR D2), z-score bucketing using all six values for real: `new_pattern` = first-ever computation for a (user, domain, metric, window); `normal`/`improving`/`deteriorating`/`anomalous` from z-score thresholds (±1.0 / ±2.5) with a per-metric `higherIsBetter` direction (sleep/mood: higher is better; stress: lower is better; training_load: deliberately direction-neutral, not guessed); `repeated_pattern` = the same non-normal signal persisting across computation runs. Explicitly does NOT attempt real temporal pattern-mining (e.g. "worse on Sundays") — a real, named gap against the enum's apparent fuller intent, not silently claimed as solved.
- [x] **Verified for real — both stated verification bars from the plan, not just claimed**: `test/worker.e2e-spec.ts` (real embedded Postgres) — (1) a well-populated metric produces exact, hand-verified mean/stddev/current_value/z-score/classification; a zero-stddev window (identical samples) doesn't divide by zero; classification genuinely evolves `new_pattern` → a real signal → `repeated_pattern` across three actual computation runs against the same underlying data. (2) **T9's must-fix, the milestone's own named concrete proof**: a second user processed in the same job run — User A recomputed three times while User B had zero rows, then User B computed — shows zero cross-tenant leakage (each user's `baselines.mean` reflects only their own, deliberately very different, seeded values). **31/31 tests passing** across all four e2e suites combined.
- [x] **Real, non-obvious bug avoided, not repeated**: almost made the exact migration-ordering mistake a third time (a new migration would need to land before `026_embeddings.js`, the deliberately-last one) — caught before running anything, no fix needed this time since Milestone 4 added no new migration at all (reused existing `baselines`/`observation_*`/`event_fitness_session` tables).
- [x] Ran the real worker (`npm run worker`, built via `nest build`) against the live persistent dev database — correctly reports "0 active user(s)" and exits cleanly, since no real user has ever actually signed in there yet (every manual curl test used a token the real Apple JWKS correctly rejected). Confirms the empty-input path doesn't crash; the e2e suite is what proves the real computation logic.
### Milestone 5 — Agent orchestration + 3 MVP specialist pairs (DONE 2026-09-21)
The largest milestone, per the plan's own framing — full build under `apps/api/src/agent/`.
- [x] **Real, documented deviation from ADR D3's literal package name — found while building, not assumed**: `@anthropic-ai/claude-agent-sdk` (the exact package ADR D3 names) is the Claude Code CLI harness repackaged as a library (session stores, git worktrees, hooks, a hard dependency on spawning the `claude` binary as a subprocess with its own settings cascade) — the right tool for a coding assistant, a poor fit for a stateless multi-tenant NestJS backend answering one `/v1/agent/ask` call at a time. What ADR D3 actually asks for (a `ModelProvider` abstraction wrapping a tool-calling loop, one file allowed to import the SDK, per-agent system prompt/model/tool-allow-list) is fully satisfied instead by the plain `@anthropic-ai/sdk` Messages API with a ~40-line hand-rolled tool-use loop. `src/agent/model-provider/anthropic-model-provider.service.ts` is the one file that imports it — same containment goal ADR D3 states, different concrete package. Worth folding back into `docs/02`.
- [x] **`ModelProvider` abstraction** (`model-provider.interface.ts`): `classifyIntent`, `runSpecialist` (drives one agent to completion via a mandatory terminal `submit_finding` tool call — far more reliable than parsing free text), `detectDisagreement`, `embedding` (throws — docs/08 §9.1 defers it entirely, never called at MVP). Two implementations: the real Anthropic-backed one, and `FakeModelProviderService` — a deterministic test double that still routes every scripted tool call through the real `ToolExecutorService`, so only the model's own reasoning is faked; RLS, confirmation tokens, audit logging, and persistence are all exercised for real in tests.
- [x] **Life Master Agent orchestrator** (`life-master-agent.service.ts`): cheap-model intent classification → `get_context_snapshot` (the orchestrator's only tool, per-domain scope-filtered with omissions logged, never an error) → **sequential** (not parallel, per docs/05 §1's explicit decision) dispatch of Health then Fitness then Mental/Emotional pairs, whichever domains were both classified relevant and scope-authorized → disagreement detection → deterministic (not model-decided) `requiresHumanReview`/`riskTier` roll-up on the composed answer → persists everything inside one `runAsUser` transaction, including a real `agent_conversations`/`agent_turns` trail (docs/08 §4.1 requires this populated in full — not previously built).
- [x] **The exact MVP Scoped Tool registry** (`tools/`): common tools (`get_domain_goals`/`_memory_digest`/`_recent_outcomes`, domain resolved server-side from the calling agent's own registration, never trusted from a model argument) plus the full Health/Fitness/Mental-Health tool sets per docs/08 §5's ceiling — every tool that ships, and explicit, documented omission (not silent absence) of every tool docs/08 defers. `cancel_scheduled_workout` (the one Tier 2+ write tool) is real: ownership-rechecked update, a real `actions` row, deliberately **not** exposed to a specialist's own model-tool-loop (only reachable through the dedicated confirm endpoint) so a live model call can never mint its own "confirmed" action.
- [x] **The 8-step chain, for real** (`tool-executor.service.ts` + `tool-registry.service.ts`): Authorization → Validation → build-time risk-tier/scope consistency assertion (T4's must-fix — asserted once at `onModuleInit` against the real `permission_scopes` catalog, confirmed passing against both the test DB and the live persistent dev DB) → Confirmation if required → Execution → Audit, unconditionally, including denials.
- [x] **T7's must-fix — real confirmation-token binding**: `ConfirmationTokenService` (HS256 via `jose`, same pattern as `TokenService`) mints a signed, 5-minute, single-use token bound to `{userId, toolName, argsHash}`; single-use is enforced by a new `agent_confirmations` table (migration `026`, not in docs/04 — a narrow, documented schema addition) whose `used_at` flip only succeeds once, inside the same transaction as execution — proven by an actual replay attempt in the e2e suite (second confirm with the same token → 403).
- [x] **T5's must-fix — untrusted-span isolation**: `get_recent_journal_raw` (the one raw free-text tool at MVP) wraps every `entry_text` value in `<untrusted_user_content>` tags; both Mental/Emotional agents' system prompts explicitly instruct that content inside those tags is data, never instructions.
- [x] **T12's must-fix — epistemic hedging**: a shared discipline block (no causal claims, no fact-shaped phrasing for `ai_inference`/`recommendation`, "not a licensed professional") appended to all three pairs' system prompts and the Life Master Agent's own composition prompt (`specialists/prompts.ts`).
- [x] **T9's must-fix — per-user rate limit**: `@nestjs/throttler`, tracked by `RequestContext.userId` (not IP) via a custom guard, on `/v1/agent/ask` only. Configured via `forRootAsync` reading env at Nest-instantiation time rather than `forRoot`'s object literal (which evaluates at module-decoration/import time) — found because the first version silently ignored the test's own tightened limit.
- [x] **Disagreement, surfaced not resolved** (`disagreement.service.ts`, docs/05 §8): self-declaration first, an independent cheap-model backstop when self-declaration doesn't flag anything, plus pairwise cross-domain checks when more than one domain was dispatched. Composition never picks a side silently.
- [x] **Real gap found and fixed while building the consumer**: nothing in Milestone 4 ever computed the `productivity`/`calendar_density_next_day` baseline docs/05 §3.6 and the worked example both need — `worker/calendar-density.service.ts` (kept separate from `BaselineService.computeForUser`, since it's a non-statistical derived flag with no window/z-score shape, and folding it in would have silently changed Milestone 4's own already-verified `{written,skipped}` counts). `classification` for this row is a documented judgment call (`anomalous` at ≥3 events tomorrow, `normal` otherwise) since none of the six enum values literally mean "busy."
- [x] **Real migration-ordering mistake, this time not avoided but hit and fixed**: the new migration was first authored to run *after* the deliberately-last `027_embeddings.js` (renumbered from 026) — it silently never ran, surfacing as a confusing tool-registry startup error ("agent.ask scope not found") rather than an obvious ordering failure. Renumbered once diagnosed; documented in both files as the third instance of this exact class of mistake.
- [x] **Verified for real — both of the milestone's own stated bars, plus more**: `test/agent.e2e-spec.ts`, real HTTP against real embedded Postgres, `FakeModelProviderService` standing in only for the live model call. Confirms: real sequential dispatch of Health then Fitness (Mental/Emotional not classified for this question, matching the worked example); the Fitness-pair disagreement (Fitness Performance: "train as planned" vs. Training Safety: "elevated fatigue risk, lighter session") is persisted with a real `disagrees_with_output_id` pointer, not just mentioned in prose; a full Tier 2 confirm→execute→audit round trip cancelling the seeded planned session, including a rejected token replay; the per-user rate limit actually 429s past its configured window. **35/35 tests passing** across all five e2e suites combined (was 31/31 before this milestone).
- [x] Ran the real built worker (now including `CalendarDensityService`) and the real API server (all agent routes registered, tier-consistency assertion passing) against the live persistent dev database — both boot and run cleanly.
- [x] **iOS**: `AskView` now has a real text-entry bar (`GlassAskBar` is a tap-to-open button elsewhere, not a text field, so a real `TextField` was added), calls `POST /v1/agent/ask`, and renders the real evidence/uncertainty/confirm-or-decline shape. `HomeView`'s Insight card is wired to the same real ask flow (a fixed canonical question, since no separate "today's proactive insight" endpoint exists at any Phase 1 milestone — a real, flagged MVP simplification, not a hidden shortcut) and its two actions run the real Tier 2 confirm/decline flow. Both `swift build` and the real `xcodebuild` app-target build succeed. Simulator-based visual verification is still blocked on the same pending runtime download as every other iOS milestone this session.
- [x] **Honestly flagged, not fixed**: the actual Anthropic SDK integration has never been exercised against a live model in this environment (no `ANTHROPIC_API_KEY` configured, same category as the Sign-in-with-Apple/HealthKit entitlement blockers) — every other part of the system (tool chain, RLS, confirmation tokens, disagreement detection, persistence, audit, rate limiting, both real builds) is verified for real; only the live model call itself is not. `get_domain_memory_digest` returns the user's whole active memory set rather than a true per-domain slice, since `memories` carries no domain column at MVP (docs/08 §4.1) — documented in the tool's own code, not silently narrowed.

### Milestone 6 — Privacy & data-control (DONE 2026-09-21)
Full build under `apps/api/src/privacy/`.
- [x] **Real gap found while building the consumer**: nothing populated `integrations` rows for Apple Health/Calendar connections (docs/08 §4.1 assumed it would be) — the account-deletion workflow's step 1 ("revoke every integrations row") would have been a silent no-op. Fixed at the source: `HealthSyncService`/`CalendarSyncService` now upsert a `connected` row on every successful sync.
- [x] **A real object-storage substitution, same pattern as Milestone 5's ModelProvider finding**: ADR D6 specifies S3; there is no live AWS account in this environment. `ExportBundleStorageService` implements the exact same contract (put a versioned blob, mint a short-lived signed download reference, delete a specific version or every version) against the local filesystem — a real, working store for this dev environment, with the `documents` table already shaped exactly for a real S3 client to slot in at Milestone 8 without touching any caller.
- [x] **Data export bundle** (`export-bundle.service.ts`): a genuinely valid ZIP (verified via its own local-file-header magic bytes and via `unzip` directly against a throwaway file while building this), built from real queries against every MVP-populated table, matching docs/08 §7's exact reduced folder set — `profile/`, `goals/`, `health/`, `fitness/`, `mental_health/` (gated), `memories/`, `ai/`, `audit/` — with every other domain's directory simply absent, not an empty stub. `mental_health/` requires a real two-step confirmation (reusing Milestone 5's own single-use, time-boxed confirmation-token service) before it's included — a genuine extra proof-of-intent step, honestly **not** the fresh Apple/passkey re-authentication docs/07 §8 literally describes; flagged, not silently substituted.
- [x] **Account deletion, the real 8-step workflow** (`account-deletion.service.ts`, docs/07 §7): a 14-day grace period (`pending_deletion`, cancellable — found while building this that cancelling requires signing back in first, since step 2 revokes every existing session by design) or immediate deletion. Hard-deletes every active/derived row for the user across the full table set (not just MVP-populated ones), in a dependency order found by actually running it against real Postgres — several non-cascading foreign keys (`agent_output_sources.source_output_id`, `outcomes.outcome_of_id`, `relationships.subject_id`/`object_id`, `decisions`'s outgoing references) would otherwise raise real FK violations, not assumed safe from reading the schema doc alone. `permission_grants`/`consent_records`/`audit_log` are correctly never touched (retained indefinitely per docs/07 §5).
- [x] **Real doc/DDL conflict found and resolved, same category as 020/024/025/026**: docs/07 §7 step 8 says to null `apple_sub` on anonymization, but docs/04's actual DDL declares it `UNIQUE NOT NULL` — nulling it would violate the column's own constraint. Resolved with a deterministic, content-free tombstone (`'deleted-' || id`) that satisfies both the constraint and the actual privacy intent.
- [x] **A real, narrow schema addition**: `users.deletion_requested_at` (migration `027`) — no document names a column for when the grace period started, which a future scheduled finalize-job (not built; no scheduler infra exists yet, same category as the worker's own "an external scheduler calls it repeatedly") needs to find accounts whose window has elapsed. Numbered correctly before `028_embeddings.js` on the first attempt this time — caught in review before running anything, unlike Milestone 5's own version of this exact mistake.
- [x] **Consent** (`consent.service.ts`, docs/07 §4.1): the three MVP consent types, with real withdrawal effects — revoking `health_data_processing`/`mental_health_data_processing` actually revokes the matching `permission_grants` rows (proven by a subsequently-403ing endpoint, not just a flipped database flag); revoking `tos` is treated as an account-deletion trigger, not a feature toggle, exactly as docs/07 specifies. A fresh signup has no explicit consent row yet (no onboarding consent UI exists at any milestone) — `list()` computes an honest implied-`granted` value from whether the equivalent scope is currently active, rather than misreporting "not granted" for a user already using the feature.
- [x] **Permission-grant listing + revoke** (`permission-grants.service.ts`): real listing and revocation of a user's own `self_app` grants, proven end-to-end by revoking `agent.ask` directly and confirming `/v1/agent/ask` then 403s.
- [x] **Verified for real**: `test/privacy.e2e-spec.ts`, real HTTP against real embedded Postgres. Confirms every consent/grant/export/deletion behavior above, plus the milestone's own two named bars: a real, downloadable, structurally-valid ZIP bundle, and account deletion actually removing/anonymizing rows end-to-end with a second user's data proven completely untouched (the same cross-tenant proof pattern Milestone 1's own RLS test established, applied here to a destructive operation instead of a read). **43/43 tests passing** across all six e2e suites combined (was 35/35 before this milestone).
- [x] Ran the real API server (all privacy routes registered) and applied the new migrations against the live persistent dev database.
- [x] **iOS**: `PrivacyDataView`'s two placeholder screens ("not wired to a backend yet") are now real — consent toggles, a swipe-to-revoke permission-grants list, a real export flow (including the mental-health confirm prompt, opening the real downloaded bundle), and the real account-deletion flow (grace period vs. immediate, with an explicit alert for the TOS-revoke-deletes-your-account case). Both `swift build` and the real `xcodebuild` app-target build succeed.

### Milestone 7 — Security/ops hardening (DONE 2026-09-21)
Full build sweeping docs/09's own T1–T12 must-fix list, plus a real 8-angle security review of `apps/api/src/{auth,database,agent,privacy}` and `apps/api/migrations`.
- [x] **Least-privilege DB roles** (`028_least_privilege_worker_role.js`): `plos_worker` tightened from broad CRUD to `SELECT` on the 8 tables the worker actually reads (`users, observations, observation_sleep, observation_journal_entry, events, event_fitness_session, event_calendar_item, baselines`) plus `INSERT, UPDATE` on `baselines` only, via explicit `REVOKE ALL` first. Also revoked `plos_app`/`plos_worker`'s write access to `permission_scopes` (a seed-once catalog, not a runtime-written table). **Real gap found while building this**: `plos_worker` had never actually been used by any application code path before this milestone — nothing connected as that role, so its grants (broad or narrow) had never been exercised against a real query. Closed by switching `worker.e2e-spec.ts` to connect as `plos_worker` for real, and by running the real worker against the live persistent dev DB as `plos_worker` after applying the migration (0 users exist in that DB, but the connection/query path completed with no permission errors).
- [x] **Fixed a recurring migration-numbering mistake at its root cause**: the embeddings migration (must always run last — needs `pgvector`, unavailable in this environment) got mis-numbered relative to a newly-added migration three separate times across Milestones 5, 6, and 7 (each time either caught in review or, once, actually run and producing a confusing error). Root-caused and permanently fixed by renaming it to `999_embeddings.js` — a number far beyond any realistic future migration count — so it can never need manual renumbering again.
- [x] **A real 8-angle multi-agent security review** (3 correctness angles, reuse, simplification, efficiency, altitude, CLAUDE.md-conventions) of the auth/RLS/tool-authorization/injection surface, per docs/09's own Milestone 7 verification bar. 16 distinct, independently-verified findings reported via `ReportFindings`; the 12 most severe/cheap-to-fix were fixed in this same pass (see below), 4 lower-priority ones deliberately deferred rather than rushed. Full findings list, verdicts, and outcomes are in this session's `ReportFindings` call, not duplicated here.
- [x] **The single most severe finding, fixed at its root cause**: `ToolExecutorService.invoke()` was throwing exceptions for scope/ceiling-denial and validation-failure paths from *inside* the caller's own `runAsUser` transaction — so the audit-log row it had just written for the denial was silently erased by that same transaction's ROLLBACK, directly violating docs/05 §6's "audit always, including denials" non-negotiable. Fixed by making `invoke()` return a typed `ToolInvokeResult` (`executed | confirmation_required | denied | invalid`) instead of throwing for denials, and by auditing genuine faults (unknown tool, execution errors) on an independent, freshly-opened transaction *before* rethrowing — proven with a new regression test that revokes a scope, invokes the tool directly via `app.get(ToolExecutorService)` (bypassing the route-level `ScopeGuard`, which would otherwise mask the tool's own internal check), and asserts the denial audit row survives.
- [x] **A real IDOR test suite** (`test/idor.e2e-spec.ts`, 6 tests) specifically targeting `cancel_scheduled_workout` — the one Tier 2+ target that exists at MVP, per docs/09's own stated verification bar. Confirms both the documented-by-design behavior (requesting confirmation for another user's session mints a token blindly, since docs/05 §6's chain checks Confirmation before Execution) and the real enforcement point (the *confirm* step 404s against another user's session and leaves their data untouched, backed by RLS + an explicit `WHERE user_id` check), plus a forged-token rejection and the audit-survives-denial regression above.
- [x] **A genuine, executed backup/restore drill** (`scripts/restore-drill.js`, `npm run restore-drill`), substituting for `pg_dump`/RDS snapshots (no client tools installed in this environment, no live AWS account) with Postgres's own physical backup method: stop the dev server cleanly, filesystem-copy its data directory, restart a *separate* Postgres process against the copy, and query a known marker row back out of it. Actually run once, successfully: `RESTORE VERIFIED: marker row recovered from the backup copy` — matching `CLAUDE.md`'s own non-negotiable that backups are only "verified" once restore has actually been tested, not just described.
- [x] **`docs/11-ops-security-runbook.md`** (new): the break-glass IAM policy/procedure (no standing superuser credential; Identity Center roles assumed via a reviewed procedure; CloudTrail logged to a separate account so a compromised prod admin can't erase its own trail; KMS key-usage vs. key-administration separation), a CloudTrail/GuardDuty checklist for Milestone 8 (genuinely blocked — no live AWS account exists in this environment), the least-privilege role rationale, and the backup/restore verification cadence. Written as a concrete document, not fabricated Terraform/CloudFormation, given the stated environment constraint.
- [x] **Other real bugs found and fixed in the same review pass**: a pre-existing (Milestone 2) passkey clone-detection bypass (`newCounter <= storedCount && newCounter !== 0` skipped the check whenever the *new* counter was 0, regardless of the stored value) — fixed, and discovered along the way that `@simplewebauthn/server` itself already enforces the same invariant internally (throwing a raw `Error` rather than returning `verified:false`), so `passkey.service.ts` now catches and converts that too; a prompt-injection-relevant bug in `wrapUntrusted()` (didn't escape `<`/`>` in untrusted content, so a crafted string could close its own delimiter early); two real data/UX bugs in account deletion (`relationship`/`outcome` core_objects rows were never in the final hard-delete list, leaving orphaned spine rows forever; `webauthn_credentials` was deleted immediately even for a cancellable grace-period request, permanently destroying a passkey a user might still restore); the `999_embeddings.js` migration silently re-granting `plos_worker` the exact privileges `028` had just revoked; a non-deterministic baseline query missing `ORDER BY`; three small reuse/efficiency cleanups (`domainOf`/`commonToolScope` duplication, `AgentActionsService`'s duplicated request/confirm bodies, `get_context_snapshot`'s up-to-4-sequential-queries-per-call collapsed to one — the hottest path in the whole agent stack, since every `/v1/agent/ask` call goes through it).
- [x] **Deliberately deferred, not silently dropped** (reported via `ReportFindings` with `outcome: skipped`, each with a named reason): pre-`ToolExecutorService` auth/guard denials (failed logins, scope-guard route rejections) still have no audit trail — a real, systemic gap distinct from the one fixed above, but a bigger design decision (auditing pre-authentication failures with no known `user_id` yet) than this pass's time budget covered; `TenantDatabaseService`'s three near-identical transaction-wrapper methods; the JWT sign/verify pattern independently implemented three times (confirmation tokens, session tokens, export-download signed URLs); the mental-health export confirmation flow's own hand-rolled confirm/deny branches aren't audited the way `ToolExecutorService`'s chain is.
- [x] **`npm audit`**: re-checked against Milestone 1's documented baseline (7 high-severity advisories, all in dev/build tooling). Found and fixed one that had gotten worse since then — `qs` (a real runtime dependency of Express's request/query parsing, not just build tooling) was pinned two patch releases behind a fix via a transitive `body-parser@1.20.4` pin from `@nestjs/platform-express@10.4.22`. Fixed with a non-breaking `package.json` `overrides` block (`body-parser@^1.20.8`, `qs@^6.16.0`) rather than force-upgrading NestJS's major version — confirmed via `npm ls qs` that every consumer (Express, body-parser, supertest) now resolves to the patched version. Remaining 20 advisories are all in `@nestjs/cli`'s dev-only build toolchain (webpack, picomatch, tmp/inquirer) or `multer` (bundled, unused — no file-upload route exists yet), matching the same "not a real runtime path" shape as Milestone 1's baseline; fixing them needs `@nestjs/cli`'s next major version, deliberately not forced in this pass.
- [x] **Verified for real**: `npm run test:rls` — **51/51 tests passing** across 7 e2e suites (was 43/43 before this milestone), including the new IDOR suite and the audit-survives-denial regression, run three consecutive times after the last fix with no flakes. `nest build` succeeds after the dependency override. The new migrations were applied to the live persistent dev database (`npm run dev:db`) and the real API server (`npm run start`) booted cleanly against it with every route registered and the tier-consistency assertion passing; the real worker ran against that same live DB connected as the tightened `plos_worker` role.
- Not done, matching what this milestone's own text scoped out: CloudTrail/GuardDuty are checklist items in `docs/11`, not live AWS resources — no AWS account exists in this environment (Milestone 8's job). The systemic pre-auth denial-audit gap is flagged, not fixed, per above.

### Pre-Milestone-8 audit pass — code leanness, safety/accessibility, network hardening, UI/UX (DONE 2026-09-21)

All four items actually run (not just planned). Backend verified with the real test suite (build + 51/51 `test:rls`, run repeatedly after each batch of fixes); iOS verified with a real `xcodebuild` simulator build (**BUILD SUCCEEDED**) after every batch of changes, plus a live simulator boot/inspect pass.

- [x] **Code leanness/over-engineering audit** — ran `yuval-skills:ponytail-audit` (whole-repo mode; `code-simplifier` remains uninstalled, not needed) as two parallel sweeps: `apps/api` (89 files, 6,213 lines) and `apps/ios`/`PlosDesignSystem` (31 files). Backend: no unused dependencies, no single-implementation-interface bloat (`ModelProvider` and `ScopedToolContract` both have real multi-consumer polymorphism) — 7 real findings, applied: a shared `readGrantedScopes` helper collapsing 3 duplicated permission-grant queries (`auth/read-granted-scopes.ts`), a shared `insertUserEnteredSpine` helper collapsing 4 duplicated `core_objects` spine inserts (`database/insert-user-entered-spine.ts`), a shared `validateBoundedNumber` collapsing 4 duplicated tool-arg validators (`agent/tools/tools-validation.ts`), `MODEL_IDS.cheap` reuse instead of a re-derived literal, and two dead-code deletions (`AgentOutputRepository.setDisagreesWith`, `AgentOutputCompositionSources`). **Skipped, deliberately**: `ModelProvider.embedding()` — flagged as dead by the audit, but ADR D3 (cited in docs/03, docs/04, docs/05) explicitly commits the interface to covering embeddings even before anything calls it; deleting it would contradict a named architecture decision, not just cut dead code. iOS: zero third-party SwiftPM dependencies and zero protocols exist at all, so nothing to check there — 8 real findings, applied: `PlosAPI.swift`'s dead decoded fields (`requiresHumanReview`/`requiresUserConfirmation`/`riskTier`/`sources`/`coreObjectId`/`omittedDomains`/`grantedVia`/`sections` — verified zero references anywhere first), `SuccessResponse` collapsed into the existing `SuccessLikeResponse`, dead `cancelAccountDeletion()` and `AppSession.userId` deleted, and `GlassAskBar`/`AskView`'s duplicated glass-fallback modifier collapsed into one parameterized `GlassOrMaterial(interactive:)` in `Theme.swift`. **Skipped, deliberately**: the 11x-duplicated guard/busy-flag/error-catch pattern across 8 screens (real, but a control-flow refactor with zero iOS test coverage to catch a regression) and `AskView`/`InsightCard`'s button-style extraction (real, but the two contexts use inverted color schemes — light-on-dark vs dark-on-light — and a copy/paste mistake there is a visual regression no test would catch); `SettingsView`'s unused `notificationsEnabled`/`useMetric` toggles (delete-or-wire-up is a product decision, not an audit call).
- [x] **User safety & accessibility audit**:
  - *Safety*: ran the bundled `code-review` skill's rubric directly (not a literal skill invocation — hand-scoped, since `yuval-skills:security-review` doesn't exist) against `agent/**` for the three CLAUDE.md non-negotiables. All three had real, evidenced gaps, all fixed: (1) **provenance tagging never reached the wire** — `core_objects.epistemic_status` (`fact`/`derived_fact`/`ai_inference`/`recommendation`) was enforced in the DB but evidence items and the composed output shipped to the client with no tag at all, and no core-objects read endpoint existed to resolve one; fixed by resolving each evidence item's real tag in one query (`AgentOutputRepository.attachEvidenceEpistemicStatus`) and exposing the output's own tag (`AgentOutputRepository.epistemicStatusOf`) on `AskResult`. (2) **no build-time check that a tier 2+ tool actually requires confirmation** — `riskTier` and `requiresConfirmation` were fully independent fields; fixed with `ToolRegistryService.assertTier2PlusRequiresConfirmation()`, a startup assertion alongside the existing tier-ceiling check. (3) **cross-domain disagreement (mechanism 3) was computed then discarded** — fed only into the composition model's prompt, with no durable record and no guarantee the composed prose would mention it; fixed by auditing every detected cross-domain mismatch (`agent.cross_domain_disagreement_detected`) and returning it as a new structured `crossDomainDisagreements` field on `AskResult`, not left to the model's prose. New assertions added to `test/agent.e2e-spec.ts` proving all three; 51/51 passing. Also fixed in passing: `privacy.controller.ts`'s mental-health export flow was silently missing audit-log rows on its `confirmation_required`/`denied` branches — a denied export attempt (e.g. a stolen session probing for sensitive data) left zero trace; both branches now audited.
  - *Accessibility*: real, first-ever pass on `apps/ios` — static review of all 21 screen/component files; a live simulator boot (iPhone 17, iOS 27) was started to cross-check against the rendered UI but the inspect pass itself is queued, pending the user's one-time simulator-access grant (see the verification note below). 0 Critical, 9 High findings, all 9 fixed: missing `accessibilityLabel` on AskView's send button, both CalendarView day-switcher chevrons, and the Meds/Workouts toolbar "+" buttons; JournalView's Stress/Energy sliders had no label closure (Mood already did); InsightCard's eyebrow/headline/detail had no VoiceOver grouping (fixed with a nested `.accessibilityElement(children: .combine)`, buttons kept outside the group); HistoryView had no empty/loading state at all (added `ContentUnavailableView`). Also applied several Medium findings while touching the same code: 44pt tap-target minimums on AskView/InsightCard's confirm buttons, row-level VoiceOver grouping and empty states added to MedsView/WorkoutsView to match. **Strengths worth keeping, confirmed by the audit**: zero hardcoded colors and zero fixed font sizes anywhere in the 21 files (100% routed through `PlosTheme`/semantic text styles) — `TodayStateGrid`'s composite-tile grouping and `GlassAskBar`'s overridden accessible name are the reference patterns the rest of the app should keep copying.
- [x] **Rate limiting / network hardening audit**: *application-level* — turned out to be a **stale checklist claim, not a real gap**: `AuthController` already has `@UseGuards(ThrottlerGuard)` on every pre-auth route (apple, refresh, both passkey endpoints), backed by a real named `auth` throttler (`app.module.ts`) and covered by e2e tests — this was actually built during Milestone 7 itself; the "known blockers" gap-audit note above was never updated after that shipped (now corrected there too). *Network-level (firewalls/WAF)* — confirmed, as scoped originally, that this isn't a code-audit item: no infrastructure exists yet to review; deferred to Milestone 8 provisioning `docs/11-ops-security-runbook.md`'s network section for real.
- [x] **UI/UX audit** — folded into the accessibility pass above (one static review covered both HIG anti-patterns and accessibility, since they're the same 21 files): tap targets, semantic colors, Dynamic Type, menu-vs-action-sheet usage, and empty/loading states were all checked together rather than as two separate passes over the same code. No further `design/*` sub-skill dispatch was needed — nothing surfaced a gap specific to liquid-glass, typography, or ux-writing beyond what's captured above.
- **Verified for real**: backend build clean + 51/51 `test:rls` after every batch of fixes (ran 3 times across the session). iOS: real `xcodebuild -scheme plos -destination 'generic/platform=iOS Simulator'` → **BUILD SUCCEEDED** after every batch. **A genuine first: the app was actually installed and launched on a real iPhone 17 (iOS 27) simulator** — something that had never happened before this session (Milestone 0 only got as far as a successful *build*). This surfaced and fixed a real, previously-undiscovered bug entirely separate from the audit findings above: `apps/ios/plos/Info.plist` (the custom file added during the earlier gap audit to fix `NSAllowsLocalNetworking`) was missing `CFBundleIdentifier`, `CFBundleExecutable`, `CFBundlePackageType`, `CFBundleVersion`, `CFBundleShortVersionString`, and `CFBundleName` entirely — a manually-provided Info.plist doesn't get these synthesized the way `GENERATE_INFOPLIST_FILE`'s default does, so the app built cleanly but could never actually have been installed anywhere (device, TestFlight, App Store) until now. Fixed with the standard `$(...)`-placeholder keys; confirmed via `plutil` that the built app now correctly resolves `CFBundleIdentifier` to `com.plos.app`. The app launched to LoginView correctly (screenshot taken, matches the design system — "Sign in with Apple" and passkey buttons both render at the correct size). **Limitation, disclosed rather than glossed over**: the screens the 9 accessibility fixes actually touch (AskView, CalendarView, HistoryView, MedsView, WorkoutsView) sit behind Sign-in-with-Apple/passkey auth, which this environment cannot complete (no real Apple Developer team — the same pre-existing blocker already on record). A live VoiceOver/accessibility-tree inspect of those specific screens was not possible this session; the fixes are verified by source review and a clean compile, not by live inspection past the login screen. The simulator's `inspect` action was also unavailable in this environment when tried.
- **Corrections made along the way, not just findings**: `docs/01-capability-audit.md` §3, this section, and `CLAUDE.md` all previously cited `yuval-skills:security-review` as an installed, confirmed skill — `ListSkills` returns nothing for it. All three docs corrected to point at the bundled `code-review` skill, which is what Milestone 7's actual security pass used.

### Milestone 8 — AWS staging deploy (IaC written 2026-09-22, not applied — no AWS account exists)

The user explicitly chose "write the IaC now, deploy later" over waiting for a real AWS account — a deliberate change from `docs/11-ops-security-runbook.md` §1's earlier, on-the-record stance ("writing IaC against an account that doesn't exist would be unverifiable by construction"). Both are true at once: the reasoning in that document was sound, and the user's later choice to proceed anyway is what happened — recorded here so neither reads as silently contradicting the other.

- [x] **Terraform written** (`infra/`) for the exact stack docs/09's Milestone 8 section names — RDS Postgres 16 Multi-AZ (private, encrypted, no public endpoint per ADR D4), an ALB, one `api` Fargate service, a nightly-**scheduled** (not standing) `worker` Fargate task via EventBridge Scheduler matching Milestone 4's own "a scheduled job, nightly is enough" line, S3 (versioned, SSE-KMS) for export bundles, Secrets Manager for every credential the app actually reads (checked directly against `process.env.*` usage across `apps/api/src`, not guessed), one KMS CMK, an ECR repo, and least-privilege security groups (internet → ALB → Fargate → RDS, no hop skipped). Sized per ADR D4's explicit "not a large multi-service buildout." Full file list and deploy sequence: `infra/README.md`.
- [x] **Two real, previously-undiscovered application-code bugs found and fixed while writing this** (not just infra — writing real IaC against the real code surfaced gaps a document-only pass couldn't have):
  1. `apps/api/migrations/001_roles_and_extensions.js` hardcoded the `plos_app`/`plos_worker` Postgres role passwords as plaintext literals — its own header comment already said production credentials should come from Secrets Manager, but the code never actually branched on environment. Applying this migration unmodified against a real RDS instance would have given the production database roles these exact, public, repo-visible passwords, undoing Milestone 7's own least-privilege-role work at the credential level. Fixed: now reads `PLOS_APP_ROLE_PASSWORD`/`PLOS_WORKER_ROLE_PASSWORD` from the environment (the new `migrate` Fargate task injects real Secrets-Manager-backed values), falling back to the original dev-only literals when unset — local dev/CI/the 53-test suite are unaffected (verified: all still pass).
  2. `apps/api/src/privacy/export-bundle-storage.service.ts` only ever wrote to the local container filesystem — provisioning a real S3 bucket without also fixing this would have left it completely unused, with exports silently landing in Fargate's ephemeral, non-shared storage. Fixed: added a real `@aws-sdk/client-s3`-backed path (put/read/deleteAllVersions, matching S3's own versioning semantics for the "delete every version" requirement docs/07 §7 step 5 states), switched on by `PLOS_OBJECT_STORE_DRIVER=s3`, behind the exact same interface/contract the local implementation already had — callers (`ExportBundleService`, `AccountDeletionService`) and the `documents` table shape needed zero changes, matching this file's own original design intent. **Not tested against a real S3 bucket** (none exists) — verified by build + type-check + manual review only, the same honest limit as the Terraform itself. Deliberately did *not* switch `mintDownloadToken` to S3's native presigned-URL mechanism — that would change the actual download contract (app-mediated today, via a custom short-lived JWT) and is a real design decision left for a human, not folded silently into an infra swap.
- [x] **A third, unrelated real bug found live**: `apps/ios/plos/Info.plist` (added during an earlier gap audit) was missing `CFBundleIdentifier` and every other standard bundle key — the app built cleanly but could never have been installed anywhere. Found and fixed during this same pass; full detail in the pre-Milestone-8 audit-pass section above (Milestone 0).
- [x] **A new real endpoint added**: `GET /health` (`apps/api/src/health.controller.ts`) — every existing route requires auth, which would have made the ALB's target-group health check mark every Fargate task permanently unhealthy. Does a real DB round trip (`SELECT 1` via `TenantDatabaseService.runPreAuth`), not a hardcoded 200. Tested for real (`test/health.e2e-spec.ts`, 2 tests, real embedded Postgres) — a third test asserting a 503 on genuine DB failure was attempted and dropped: killing embedded-postgres mid-test-run hung the suite's own teardown (a harness limitation, not a product bug); the happy-path test already proves the real DB code path executes.
- [x] **`npm audit` re-checked after adding `@aws-sdk/client-s3`**: 6 production-relevant advisories exist, all pre-existing and unrelated (transitively via `@nestjs/common`'s `file-type` and `@nestjs/platform-express`'s `multer` — confirmed via `npm ls`, not assumed), all requiring a NestJS v12 major-version bump to fix. Out of scope for this pass; not introduced by today's change.
- **Not done, honestly**: nothing in `infra/` has ever been applied — no AWS account exists in this environment (no AWS CLI, no credentials, confirmed by checking, not assumed). CloudTrail/GuardDuty/the break-glass IAM role remain exactly what `docs/11-ops-security-runbook.md` §3/§4 already left them as: a written, reviewed decision waiting on a real account, not Terraform. No CI/CD pipeline wraps the deploy sequence — it's a documented manual sequence (`infra/README.md`) for now. No Terraform state backend is configured (deliberately left as an empty slot in `infra/versions.tf` — a decision for whoever runs the first real `apply`, not a default to bake in silently). RLS-enforced-remotely and the restore-drill-against-real-RDS verification items from docs/09's own Milestone 8 bar cannot be checked until an account exists.

## Gap audit across Phases 0-1/M1/M2 (2026-09-21, on request: "are there any gaps from the first phases")

Not a re-summary of what's already tracked above — a deliberate, fresh
pass looking for what might have been missed. Two real, concrete gaps
were found and fixed on the spot; the rest are flagged for a decision
rather than silently fixed or silently ignored.

**Found and fixed:**
1. **No way to run `apps/api` as a live server at all, until now.** `docs/09`'s Milestone 1 plan assumed Docker Compose for local Postgres; this environment has never had Docker. Milestone 1 solved that only for *tests* (`embedded-postgres`, torn down after every run) — there was never a path to a persistent local server, meaning nothing (iOS included) could ever actually talk to a live backend here. Fixed: `apps/api/scripts/dev-db.js` + `npm run dev:db` boots the same `embedded-postgres` binary persistently, applies all migrations, and stays up. **Verified for real**: started it, started `npm run start` against it, and made the first-ever live HTTP calls this project has seen — `POST /v1/auth/passkey/login/options` returned a real, correctly-shaped response (confirmed to byte-match what the iOS `PlosAPI` Swift structs decode — the first real evidence the two sides' contracts actually agree, not just each independently self-consistent); `POST /v1/auth/apple` with a garbage token hit the **real** `https://appleid.apple.com/auth/keys` (this sandbox has outbound internet) and correctly 401'd fast — the real remote-JWKS code path, never exercised before (the e2e suite always used an injected local JWKS by design), works.
2. **iOS couldn't have reached the backend even with a live server**: no `Info.plist` existed (Xcode was auto-generating one via `GENERATE_INFOPLIST_FILE=YES`), so the default App Transport Security policy would have silently blocked every plain-HTTP request to `http://localhost:3000` the moment this was actually run. Fixed: a real `apps/ios/plos/Info.plist` with `NSAllowsLocalNetworking` (the narrow ATS exception meant exactly for local-network dev traffic, not a blanket cleartext-HTTP allowance) — confirmed baked into the built app bundle via `plutil -p` on the compiled output, not just present in source.

**Flagged, not fixed — real, but a scope/priority decision, not a bug:**
3. ~~Passkey **registration** is fully unreachable from any UI~~ — **done 2026-09-21**: `SettingsView` now has a "Security" section with an "Add a Passkey" button, wired to the real `passkeyRegisterOptions`/`passkeyRegisterVerify` endpoints and `PasskeyCeremonyRunner.register()`. Tightened while wiring it: the iOS client now decodes and reuses the server's own `user.id`/`user.name` from the registration options response instead of re-deriving a `userID` client-side (the two would otherwise not have matched, since the server derives it from the UUID directly). Still can't complete end-to-end for the same reason login can't — no Associated Domains entitlement — but the button attempts the real ceremony and surfaces whatever real error results, rather than being a disabled/hidden placeholder. Both `swift build` and the real `xcodebuild` app-target build succeed with this change.
4. ~~No application-level rate limiting on auth endpoints~~ — **done, built during Milestone 7**: `AuthController` has `@UseGuards(ThrottlerGuard)` on every pre-auth route (apple, refresh, both passkey endpoints), backed by a real named `auth` throttler in `app.module.ts` and covered by e2e tests. **Correction (pre-Milestone-8 audit, 2026-09-21)**: this item was re-flagged as still-open in the Pre-Milestone-8 audit-pass checklist below before this file caught up with what Milestone 7 actually shipped — verified against the live code, not re-asserted from this stale note.
5. Minor consistency note for whoever configures real Sign in with Apple later: the app's real bundle ID is `com.plos.app`; the backend's current placeholder default (`APPLE_AUDIENCE ?? 'com.plos.app.not-yet-configured'`) won't match it. Not a bug today (both are placeholders) — just don't forget to set `APPLE_AUDIENCE=com.plos.app` (or whatever the real Apple Service ID ends up being) when that capability is actually enabled.

**Checked and confirmed NOT gaps** (raised as a hypothesis, then ruled out):
- No `consent_records` writes exist yet for health/mental_health data being written in Milestone 2 (journal entries, medication logs). Checked against `docs/09` directly: consent records are explicitly Milestone 6 scope ("Consent records for the three types actually exercised"), not an unplanned oversight.
- The WebAuthn sign-count replay check correctly special-cases authenticators (like Apple's) that always report counter `0` — confirmed by re-reading the exact conditional, not just trusting the original comment.
- RLS/roles/audit_log integrity: re-confirmed against the live persistent dev database (not just the ephemeral per-test-run one), not merely re-asserted from the existing 20/20 suite.

## Known blockers requiring the user specifically

1. ~~Open `apps/ios/plos.xcodeproj` in real Xcode~~ — **done 2026-09-21**, builds successfully (one bug found/fixed, see Milestone 0 above). The simulator runtime finished downloading during the pre-Milestone-8 audit pass (2026-09-21) — a real device boot/launch pass is next.
2. **Enable Sign in with Apple AND HealthKit capabilities** in Xcode Signing & Capabilities with a real Apple Developer team — needed before Milestone 2's iOS-side auth testing, and now also confirmed necessary for Milestone 3's HealthKit sync to work on-device/simulator at all (verified: the HealthKit entitlement is silently dropped from the signed binary without a real team, not a code issue).
3. **Confirm the CRUD-vs-agent-tool scope reconciliation** in `docs/09-phase1-implementation-plan.md`'s Context section, if it needs adjusting.
4. **Decide sequencing for `apps/web` and `apps/android`** (new, 2026-09-21) — which gets built first, and confirm the default technology assumptions in CLAUDE.md's "Client-platform scope" section (a mainstream React-based web stack; Kotlin/Jetpack Compose for Android) before real work starts on either.

## Backlog ideas (requested 2026-09-21, not part of docs/09's milestone list, not designed or started)

- [ ] **Long-form post-signup personalization interview.** Requested directly: after signup completes, run a real, in-depth interview (not the current single-screen surface) to gather the personalization context the agent and Personal Baseline Engine need. Current state, precisely: `SignUpView` (`apps/ios/PlosDesignSystem/Sources/PlosDesignSystem/Auth/SignUpView.swift`) offers exactly four fixed goal buttons ('Sleep better' / 'Train consistently' / 'Manage stress' / 'Just exploring' — `users.service.ts`'s `GOAL_DOMAIN` map), each writing one `goals` row via `UsersController.updateProfile` — that is the *entire* onboarding personalization surface today. Open design questions before this can be scoped into a real milestone task (none answered yet, listed so the next session doesn't have to rediscover them):
  - What does the interview actually need to learn per domain (health/fitness/mental_health) that a single goal string can't capture — baseline routines, constraints/injuries, existing diagnoses, schedule patterns?
  - Where do the answers live — new columns/tables, or free-text into `memories` (docs/07's memory model already exists for exactly this kind of durable, revisable fact)?
  - Is it skippable/resumable, and does it block reaching `MainTabView`, or run lazily in the background after a minimal signup?
  - Does it feed the Life Master Agent directly (as seed `memories`/`baselines`), or only inform static goal-setting?
  - Consent implications: a "long full interview" collecting health/mental-health detail is exactly the kind of data `docs/07-privacy-model.md`'s consent-type gating already exists for — the interview's own questions likely need to be gated behind the same `health_data_processing`/`mental_health_data_processing` consent flows Milestone 6 built, not a new, separate consent path.
  - Not scoped to a milestone yet — needs a product decision on scope/length before implementation, not a "just build it" item.
